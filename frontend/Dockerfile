# Используем базовый образ Node.js
FROM node:18

# Устанавливаем рабочую директорию
WORKDIR /app

# Копируем package.json и package-lock.json
COPY package*.json ./

# Устанавливаем зависимости
RUN npm install

# Копируем остальные файлы приложения
COPY . .

# Собираем приложение (замените "build" на ваш скрипт сборки, если он отличается)
RUN npm run build

# Указываем порт, на котором будет работать приложение (замените на ваш порт)
EXPOSE 3000

# Команда для запуска приложения (замените на вашу команду)
CMD ["npm", "start"]